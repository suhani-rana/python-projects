# Password Generator

Simple Python script to generate secure random passwords.

## Features
- Choose length 8–64
- Optionally include uppercase, lowercase, digits, symbols
- Copies password to clipboard if `pyperclip` installed

## Run
```bash
python generator.py
